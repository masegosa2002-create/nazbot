---
import "@fontsource-variable/inter";
import Header from "~/components/header.astro";
import Hero from "~/components/hero.astro";
import ReviewsTicker from "~/components/reviews-ticker.astro";
import Problema from "~/components/problema.astro";
import Solucion from "~/components/solucion.astro";
import Sprint from "~/components/sprint.astro";
import Demos from "~/components/demos.astro";
import Incluye from "~/components/incluye.astro";
import Proceso from "~/components/proceso.astro";
import Faq from "~/components/faq.astro";
import CtaSection from "~/components/cta-section.astro";
import Footer from "~/components/footer.astro";
import "~/styles/index.css";

const title = "Nazbot | Sistema comercial con IA para captar, responder y seguir leads";
const description =
  "Nazbot instala sistemas comerciales con IA para responder mejor, hacer seguimiento y convertir más leads en citas o ventas.";
const siteUrl = "https://nazbot.space";
---

<!doctype html>
<html lang="es" class="h-full motion-safe:scroll-smooth" data-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.png" type="image/png" />
    <meta name="generator" content={Astro.generator} />

    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- Open Graph -->
    <meta property="og:title" content="NAZBOT — Sistema comercial con IA" />
    <meta property="og:type" content="website" />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={siteUrl} />
    <meta name="twitter:card" content="summary_large_image" />

    <!-- Preconnect -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  </head>

  <body class="h-full overflow-x-hidden bg-default text-default">
    <Header />
    <main>
      <Hero />
      <ReviewsTicker />
      <Problema />
      <Solucion />
      <Sprint />
      <Demos />
      <Incluye />
      <Proceso />
      <Faq />
      <CtaSection />
    </main>
    <Footer />

    <!-- Scroll Reveal -->
    <script>
      const revealElements = document.querySelectorAll(".reveal");

      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("visible");
              observer.unobserve(entry.target);
            }
          });
        },
        {
          threshold: 0.12,
          rootMargin: "0px 0px -40px 0px",
        },
      );

      revealElements.forEach((el) => observer.observe(el));
    </script>
  </body>
</html>
